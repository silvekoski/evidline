{
  "name": "trustworthy-process-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/*\" dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "test": "vitest run",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "canary": "tsx scripts/canary.ts",
    "build-demo": "tsx scripts/build-demo.ts",
    "replay": "pnpm --filter @tpm/server replay",
    "golden": "pnpm --filter @tpm/server golden",
    "build-demo-corpus": "tsx scripts/build-demo-corpus.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@napi-rs/canvas": "^1.0.9",
    "@types/node": "^26.6.2",
    "eslint": "^9.39.5",
    "globals": "^16.0.0",
    "jszip": "^3.10.2",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.69.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@tpm/adapters": "workspace:*",
    "@tpm/core": "workspace:*",
    "@tpm/egress": "workspace:*",
    "@tpm/schemas": "workspace:*"
  }
}
